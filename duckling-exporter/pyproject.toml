[project]
name = "duckling-exporter"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.5.1",
    "psycopg2-binary>=2.9",
    "pyiceberg[s3fs]>=0.7.0",
    "pyarrow>=14.0.0",
]
